import { ActionTypes } from "../actionTypes/action-types"

export const setDashboardLoading = (dashboardLoading) => {
    return {
        type:ActionTypes.DASHBOARD_LOADING,
        payload: detailedUser
    }
}